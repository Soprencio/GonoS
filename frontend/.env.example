# Solo necesario para build de producción (cuando el frontend no se sirve
# desde el mismo dominio que el backend). En desarrollo el proxy de Vite
# maneja las rutas /api automáticamente.
VITE_API_URL=https://tu-backend.railway.app/api
